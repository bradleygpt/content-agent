"""content-agent — turns markets-llm's measured analysis into publishable content.

Read-only consumer of markets-llm (engine artifacts, run outputs, event registry); never writes to it.
Voice: measured evidence in a world of confident noise — every piece built on one computed study, weighted
two-sided, every honesty label carried, no calls, no predictions.
"""
